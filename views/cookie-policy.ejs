<% title = typeof title !== 'undefined' ? title : t('cookiePolicy.title'); %>

<section class="card cookie-policy-card">
  <h1><%= t('cookiePolicy.title') %></h1>
  <p><%= t('cookiePolicy.intro') %></p>

  <h2><%= t('cookiePolicy.whatTitle') %></h2>
  <p><%= t('cookiePolicy.whatText') %></p>

  <h2><%= t('cookiePolicy.necessaryTitle') %></h2>
  <p><%= t('cookiePolicy.necessaryText') %></p>

  <h2><%= t('cookiePolicy.consentTitle') %></h2>
  <p><%= t('cookiePolicy.consentText') %></p>

  <h2><%= t('cookiePolicy.manageTitle') %></h2>
  <p><%= t('cookiePolicy.manageText') %></p>
</section>
